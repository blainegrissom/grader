package main.java.assignments;

public interface AssigmentInterface {
    void bonusQuestions(int bonusQuestions);
    void weight(double weight);
    void assignmentType(Enum assignmentType);
    void totalQuestions(int totalQuestions);
}
